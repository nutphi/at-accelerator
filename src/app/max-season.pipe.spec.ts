import { MaxSeasonPipe } from './max-season.pipe';

describe('MaxSeasonPipe', () => {
  it('create an instance', () => {
    const pipe = new MaxSeasonPipe();
    expect(pipe).toBeTruthy();
  });
});
