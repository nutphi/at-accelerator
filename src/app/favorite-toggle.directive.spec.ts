import { FavoriteToggleDirective } from './favorite-toggle.directive';

describe('FavoriteToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new FavoriteToggleDirective();
    expect(directive).toBeTruthy();
  });
});
